const fs = require('fs');
const path = require('path');
module.exports = (filePath)=>{
    try {
        // const pathArr = filePath.split('')
        console.log();
    } catch (error) {
        
    }
}